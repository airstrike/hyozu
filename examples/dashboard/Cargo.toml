[package]
name = "dashboard"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hyozu = { path = "../.." }
sweeten = { path = "../../../sweeten" }
iced = { version = "0.15.0-dev", features = ["canvas"] }
